import React from 'react'
import { movieData } from '../dummy/data'

const Home = () => {
  return (
    <div>
      {movieData.map(() => {
        return
      })}


    </div>
  )
}

export default Home
